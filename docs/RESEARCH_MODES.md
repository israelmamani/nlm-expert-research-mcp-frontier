# Modos

`AUTO` escala según suficiencia; `INSTANT` hace una pasada; `STANDARD` es la ruta normal; `DEEP` busca límites/conflictos; `VERIFY` prioriza soporte; `ULTRA` y `FORENSIC` aumentan pasadas, gaps y QA. Todos mantienen `corpusPolicy=LOCKED`.
